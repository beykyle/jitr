# Legacy examples (pre-2.0 solver engine)

`coupled.py` targets the removed `jitr.rmatrix` coupled-channels solver
and will not run against current `jitr` (last working release: the final
1.x tag). Coupled channels were an explicit non-goal of the lax-engine
rewrite; this example is kept for reference until upstream `lax` grows
coupled-channel support.
